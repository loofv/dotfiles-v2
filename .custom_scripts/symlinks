# !bin/sh
ln -s -f ~/git/dotfiles-v2/compton.conf ~/.config/compton/compton.conf
ln -s -f ~/git/dotfiles-v2/init.vim ~/.config/nvim/init.vim
ln -s -f ~/git/dotfiles-v2/i3config ~/.config/i3/config
ln -s -f ~/git/dotfiles-v2/.aliases ~/.aliases
ln -s -f ~/git/dotfiles-v2/.zshrc ~/.zshrc
ln -s -f ~/git/dotfiles-v2/.custom_scripts/startup ~/.custom_scripts/startup

# configs to copy:
# init.vim
# i3
# .custom_scripts/startup
# .aliases
# .zshrc
# Anything else? Double check.
# Maybe oh my zsh theme, i never change that tho.
# Make sure nothing crashes by testing all of the above.
